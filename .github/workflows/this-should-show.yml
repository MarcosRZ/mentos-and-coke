
name: THIS SHOULD SHOW

on:
  workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Create comment
        run: |
          echo "⚠️ This is a test 💣"
          exit 0
