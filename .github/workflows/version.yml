# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: [ "json-action" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read config
        id: version_step
        run: |
          content=`cat ./dev.config`
          # the following lines are only required for multi line json
          content="${content//'%'/'%25'}"
          content="${content//$'\n'/'%0A'}"
          content="${content//$'\r'/'%0D'}"
          # end of optional handling for multi line json
          version="${{fromJson(steps.version.outputs.config).termsOfService.version}}"
          echo "data=$version" >> $GITHUB_ENV

      - name: Read Terms text
        id: terms_step
        run: |
          terms=`cat ./terms.md`
          echo "data=$terms" >> $GITHUB_ENV

      - name: Create event
        id: create_event
        run: |
          echo "La versión es ${{ steps.version_step.outputs.data }}"
          echo "Los terms son son ${{ steps.terms_step.outputs.data }}"
          
      